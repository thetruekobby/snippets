name: Docker Image CI

on:
  push:
    branches: [ "main" ]
 
jobs:

  build:

    runs-on: [aws-ec2]

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker-compose up -d 
